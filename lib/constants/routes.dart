/* 
The application's top-level routing table is in the MaterialApp constructor, as a Map with a String key:

    routes: {
      ROUTE_HOME: (context) => HomePage(),
      ROUTE_LOGIN: ...
    }

*/
const ROUTE_HOME = "/home";
const ROUTE_LOGIN = "/login";
const ROUTE_REGISTER = "/register";
const ROUTE_NOTES = "/notes";
const ROUTE_VERIFY = "/verify";
